# Word Documents Fixtures

Self-tests generate minimal DOCX/OOXML packages in temporary folders. The generated cases cover document text, comments, tracked changes, unsafe ZIP paths, dry-run replacement, and missing renderer fallback.
