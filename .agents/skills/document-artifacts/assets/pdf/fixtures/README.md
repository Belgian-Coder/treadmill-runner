# PDF Processing Fixtures

Self-tests generate small PDFs in temporary folders so the repository does not need committed binary fixtures. The generated cases cover selectable text, malformed PDFs, form dry-run shape, and missing renderer fallback.
