---
id: HAR-04
epic: HAR
priority: P0
phase: 0b. Harness, foundation and delivery
tags: []
---

# HAR-04 — As an agent, navigation files (`ai/navigation/modules.md`, `specs.md`, `stories.md`) are g

## Goal
As an agent, navigation files (`ai/navigation/modules.md`, `specs.md`, `stories.md`) are generated by `./gradlew aiContext`, and `check` fails when they are stale.

## Context
- Epic: HAR — AI harness and project setup (first)
- Priority note: P0
- Spec:
- `docs/spec/00-plan.md#12-ai-harness-and-story-workflow`

## Acceptance criteria
- [ ] AC1 *[auto]*: adding a module or story without regenerating fails `check`.

## Mockups
n/a

## Screens to capture during validation
n/a

## Out of scope
- Anything not needed for the acceptance criteria above (private use, keep it simple).

## Notes for the planner
- Read the linked spec sections only; create `plan.md` from the template, with packets sized for a cheaper executor.
